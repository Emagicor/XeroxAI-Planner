# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# PDF → image (uses pymupdf, no poppler dependency)
PyMuPDF==1.24.14

# Image processing
Pillow==11.0.0
numpy==2.1.3

# Deskew
deskew==1.5.1

# Data / validation
pydantic==2.10.0
pydantic-settings==2.6.0

# Security / file scanning (MIME via file signatures — no libmagic on Windows)
pypdf==5.1.0

# Vision providers
google-generativeai==0.8.3
openai==1.57.0
huggingface_hub>=0.26.0

# Florence-2 (optional — only needed when VISION_PROVIDER=florence2)
torch>=2.1.0
transformers>=4.46.0
accelerate>=0.34.0
einops>=0.8.0
timm>=1.0.0

# Retry / resilience
tenacity==9.0.0

# Logging
structlog==24.4.0

# Export
openpyxl==3.1.5

# Env
python-dotenv==1.0.1